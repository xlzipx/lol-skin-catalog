requests>=2.31
Pillow>=10.0
openpyxl>=3.1
reportlab>=4.0

# only needed to build the standalone executable (python build.py)
# pyinstaller>=6.0

# only needed so the smoke test can verify the PDF's internal links
pypdf>=4.0
